import 'package:flutter/material.dart';
class villas extends StatelessWidget {
  const villas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Villas & Stays"),
      ),
    );
  }
}
